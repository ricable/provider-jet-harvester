apiVersion: image.harvester.jet.crossplane.io/v1alpha1
kind: Image
metadata:
  name: image-crossplane
spec:
  forProvider:
    description: "Managed with Crossplane Harvester Provider (generated with Terrajet)"
    displayName: "image-crossplane"
    sourceType: registry
  providerConfigRef:
    name: default
