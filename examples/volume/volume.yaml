apiVersion: volume.harvester.jet.crossplane.io/v1alpha1
kind: Volume
metadata:
  name: volume-crossplane
spec:
  forProvider:
    description: "Managed with Crossplane Harvester Provider (generated with Terrajet)"
  providerConfigRef:
    name: default
